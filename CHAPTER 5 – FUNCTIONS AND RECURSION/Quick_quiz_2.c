#include <stdio.h>
#include<math.h>

int main()
{    
    int a;
    printf("Enter the value of a");
    scanf("%d",&a);
    printf("The Area of Square is %f",pow(a,2));
    
    return 0;
}